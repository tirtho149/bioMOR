# ---- large data (regenerated via genomic_dataloader / tools/convert_capsule_to_csv.py) ----
data/
*.csv.gz
*_genes.csv

# ---- large binary assets ----
*.mat
*.npy
2507.10524v1.pdf

# ---- third-party genomap clone (external package; has its own large .mat + .git) ----
genomap/

# ---- python / env ----
.venv/
__pycache__/
*.pyc
*.egg-info/

# ---- backups, logs, slurm output, OS cruft ----
_cpu_backup/
logs/
*.out
.DS_Store

# keep: source, scripts (*.sbatch/*.sh), paper/ (incl. compiled PDF), results*/ JSON+CSV,
# markers_top.csv, requirements.txt, *.md, paper_review/

# ---- stray training residue ----
catboost_info/

# interaction-ablation scratch (throwaway per-task JSONs from run_task)
results_interaction/_scratch/

results_earlystop/_scratch/

# ---- baseline reproduction (lit_pipeline) ----
# third-party baseline clones: each has its own .git + multi-GB checkpoints.
# Recreate via lit_pipeline/baseline_repos/_clone_list.txt + _fetch_weights.py.
# Keep the loose adapter/setup scripts (run_*.py, _setup_*.sh, _build_*.sh, *.txt).
lit_pipeline/baseline_repos/*/
# ~1GB of downloaded review PDFs; regenerate via lit_pipeline/get_pdfs.py
lit_pipeline/pdfs/
lit_pipeline/.pytest_cache/
# per-task scratch work dirs from the deep-baseline runs (h5ad/arrow/trainer state)
results_dl_baselines/_*_work/

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
s41467-023-36383-6.pdf
results_cohorts_ms/

# large raw data + copyrighted reference PDFs (not for the repo)
new data/pancan_raw/
TableNeeded*

# raw genomap source data (materialized via git-lfs from data-branch; large)
genomap_data/

# ---- archived old results/sbatch/PDFs (~16GB local scratch; not for the repo) ----
archive/
